import XCTest

final class ScoutTests: XCTestCase {
    func testScaffoldSanity() {
        XCTAssertTrue(true, "T001 scaffold: one trivial passing test so the ScoutTests target compiles and runs.")
    }
}
