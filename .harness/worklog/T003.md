# T003 — Design tokens: ScoutTheme (colors + motion constants)

## 2026-07-17

**Status:** Done

**What was done:**
- Created `Sources/DesignSystem/ScoutTheme.swift` with all design tokens from the handoff.
- Implemented `Color(scoutHex:)` private extension to convert 24-bit hex values to sRGB `Color` objects.
- Defined `enum ScoutTheme` with 8 color constants (background, great, usable, poor, uploadArc, uploadText, onAccent, and white helper).
- Defined `enum ScoutMotion` with 6 timing constants (splashSweepPeriod, dialSweepPeriod, pulsePeriod, arcDuration, colorDuration, tickInterval).
- Added comprehensive doc-comments to each constant explaining its purpose and usage.

**Checks run:**
- `xcodegen generate` — generated Scout.xcodeproj successfully.
- `./tools/bin/swiftformat Sources Tests --lint` — passed (0 files require formatting).
- `./tools/bin/swiftlint --strict` — passed (0 violations).
- `xcodebuild test -project Scout.xcodeproj -scheme Scout -destination "name=Scout-Sim" CODE_SIGNING_ALLOWED=NO` — passed (1 test executed, 0 failures).

All Definition of Done checks passed. The file is ready for later tasks to reference these constants.
