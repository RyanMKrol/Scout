# T012 Worklog

## 2026-07-17

### Work Done
Created `Sources/Screens/ConsentView.swift` with full-fidelity first-run consent screen:
- Full-screen dark background with top-aligned content
- RadarMarkView(style: .consent) — 76×76pt with static green sweep wedge
- Title "Scout uses cellular data to measure" — 30pt bold, tracking(-0.6), white
- Body text with correct line spacing (~1.55 relative)
- Three green-dotted bullets with proper spacing and formatting
  - First bullet has bold monospaced "10–40 MB per minute" using Text concatenation + fontWeight + monospacedDigit()
  - All bullets properly aligned with 6pt green dots
- Primary button "Start sweeping" — full width, 56pt height, green fill, 16pt corner radius
- Secondary button "Not now" — 44pt height, centered, light gray text
- Content scrolls in ScrollView at large accessibility sizes; buttons stay pinned to bottom
- All accessibility identifiers in place (consent.title, consent.start, consent.notNow)

Updated `Sources/App/RootView.swift`:
- Changed `.consent` case to use `ConsentView` instead of `ConsentStubView`
- Removed obsolete `ConsentStubView.swift`

### Verification
- ✅ xcodegen generate passes
- ✅ SwiftFormat --lint passes (0 files require formatting)
- ✅ SwiftLint --strict passes (0 violations)
- ✅ All 88 unit tests pass (AppRouterTests, DomainTests, etc. all green)
- ✅ Visual screenshot with `SCOUT_LAUNCH_ARGS="-firstRunConsentGiven NO" ./build_run.sh`:
  - Shows exact title, body, three bullets with bold MB figure
  - Green primary button and quiet "Not now" secondary
  - Small static radar mark positioned correctly
  - No letterbox (launch screen present in Info.plist)
  - Layout and spacing match design spec exactly

### Status
**Done.** All Definition-of-Done checks pass. Router tests confirm closures route correctly (startSweeping / declineConsent). Screen visually verified against spec.
