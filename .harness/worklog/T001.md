# T001 — worklog

## 2026-07-16 — scaffold completed by hand (needs-human), interactive session

**What was done**
- Source tree: `Sources/ScoutApp.swift` (@main), `Sources/ContentView.swift` ("Hello, Scout" on
  near-black), `Tests/ScoutTests.swift` (one passing XCTest). `Sources/Info.plist` is XcodeGen-generated
  and now git-ignored.
- DoD tooling: `.swiftformat` (minimal, close to defaults) + `.swiftlint.yml` (lenient start; strict-clean).
- Dedicated simulator created: `Scout-Sim` (iPhone 16, iOS 26.5) — UDID
  `A656692F-4FF9-4240-8D1A-54BBC3226E93`, pinned into `harness.env` `LOCAL_DOD` destination per root
  CLAUDE.md tooling notes.
- **DoD command fix:** SwiftFormat 0.61's CLI rejects `swiftformat --lint Sources Tests` ("--lint argument
  does not expect a value" with two paths after the flag). Standardized on `swiftformat Sources Tests
  --lint` in `ci.yml`, `harness.env` `LOCAL_DOD`, root `CLAUDE.md`, and this task's spec — local and CI
  stay mirrored.

**Checks run**
- `xcodegen generate` ✅; `swiftformat Sources Tests --lint` ✅ (0/3 files); `swiftlint --strict` ✅
  (0 violations); `xcodebuild test` on Scout-Sim ✅ (1 test, 0 failures).
- `./build_run.sh` ✅ — `screenshots/latest.png` inspected: full-screen render (content behind status
  bar/Dynamic Island), NO letterbox bars — `UILaunchScreen: {}` reaching the built Info.plist confirmed.
- CI on branch + main: see PR-less merge commits on `main` (recorded below once green).
