# T017 Worklog

## 2026-07-17

**Implementation completed.**

### What was done:
1. Created `Sources/Live/CellularPathMonitor.swift` implementing `CellularPathMonitoring` protocol using `NWPathMonitor(requiredInterfaceType: .cellular)`
2. Implemented `availability()` method returning `AsyncStream<Bool>` that:
   - Consumes `NWPathMonitor` as an `AsyncSequence`
   - Maps `.satisfied` → `true`, other statuses → `false`
   - Yields current path promptly on subscription (satisfying protocol contract)
   - Cancels task and monitor on stream termination
3. Wired live implementation in `AppEnvironment.pathProvider()` method alongside the radio provider's pattern:
   - Device builds (`#if !targetEnvironment(simulator)`) without scenario argument use `CellularPathMonitor()`
   - Simulator builds use `SimulatedPathMonitor` for consistency
4. Extracted shared helper method for cleaner composition

### Checks run:
- ✅ `xcodegen generate` — project regenerated successfully
- ✅ `./tools/bin/swiftformat Sources Tests --lint` — 0 files require formatting
- ✅ `./tools/bin/swiftlint --strict` — 0 violations found
- ✅ `xcodebuild test` on Scout-Sim — 88 tests passed
- ✅ Simulator behavior test: `SCOUT_LAUNCH_ARGS="-firstRunConsentGiven YES -ScoutScenario Great" ./build_run.sh` — app launched showing measuring screen (simulated path available as expected)

**Status:** Ready for device gate (real airplane-mode behavior human-verified per spec).
