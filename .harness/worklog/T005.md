# T005 — Rolling throughput window (pure, transfer-time-weighted) — Worklog

## 2026-07-17 — Completed

**Task:** Implement `ThroughputWindow` struct for computing rolling-window Mbps calculations with active transfer-time weighting.

**Implementation:**
- Created `Sources/Domain/ThroughputWindow.swift` with:
  - `ThroughputWindow` struct: Sendable, value-type with default 2-second window
  - `record()` method: captures sample (bytes, transfer duration, end timestamp); drops invalid samples (zero/negative bytes or duration)
  - `megabitsPerSecond(at:)` method: returns Mbps weighted by transfer time only, not wall-clock time; evicts stale samples; returns nil if no valid samples remain
  - Eviction logic: samples older than `now - window` are removed (using `<=` boundary condition)
  - Helper `Duration.timeInterval` extension: converts Duration to Double seconds for Mbps calculation

- Created `Tests/ThroughputWindowTests.swift` with 13 comprehensive tests:
  - Single sample: 250KB/0.2s → 10.0 Mbps ✓
  - Two samples weighted by transfer time: 7.5 Mbps ✓
  - Sample eviction: 3s-old sample evicted with 2s window ✓
  - All samples stale: returns nil ✓
  - Invalid samples ignored: zero/negative byte counts and durations ✓
  - Paced pattern: 4×256KB/0.1s at 0.5s intervals → 20.48 Mbps (not diluted by wall-clock gaps) ✓
  - Empty window: returns nil ✓
  - Multiple samples: arithmetic correct ✓
  - Boundary condition: sample exactly at window edge is evicted ✓
  - High rate: small duration + large bytes → 160 Mbps ✓

**Concurrency handling:**
- Type marked `Sendable` (pure data, no shared state)
- All public methods marked `nonisolated` to work in any context (tests run under MainActor)
- Private helper methods similarly marked `nonisolated`
- No force-unwraps or unsafe patterns

**Verification:**
- All 26 tests pass (13 new + 12 existing domain tests + 1 scaffold)
- SwiftFormat --lint: 0 violations
- SwiftLint --strict: 0 violations
- xcodebuild test on Scout-Sim: PASS

**Spec compliance:**
- ✓ Transfer-time-weighted Mbps: `(Σ byteCount × 8) / (Σ transferDuration in seconds) / 1_000_000`
- ✓ Idle gaps between probes do NOT dilute rate
- ✓ No owned clock; time injected via `ContinuousClock.Instant`
- ✓ Pure struct, deterministic, hermetic tests
- ✓ All "Done when" assertions met and tested

No follow-up work needed.
