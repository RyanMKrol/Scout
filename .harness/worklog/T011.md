# T011 — Splash screen (full fidelity)

## 2026-07-17

### Work completed

Replaced `SplashStubView` with a full-fidelity `SplashView.swift` implementation per the design handoff.

**Files created:**
- `Sources/Screens/SplashView.swift` — complete splash screen with all specified elements

**Files modified:**
- `Sources/App/RootView.swift` — replaced `SplashStubView()` with `SplashView()`

### Layout implementation

- Full-screen ScoutTheme.background with `.ignoresSafeArea()`
- Centered VStack containing:
  - RadarMarkView(style: .splash) with 44pt bottom margin (marked `.accessibilityHidden(true)`)
  - "Scout" wordmark: 48pt bold, tracking -1.5, white, with accessibility identifier `splash.wordmark`
  - "Find your signal." tagline: 17pt medium, white at 0.5 opacity, 12pt below wordmark
- Bottom section (64pt from edge), centered with 18pt gap:
  - Loader bar: 132×3pt Capsule with white(0.12) track, 52.8pt (40% width) green fill, also Capsule shape (marked `.accessibilityHidden(true)`)
  - Honest line: "Measures real usable speed — not signal bars", 12pt, white at 0.3 opacity

### Verification

**With hold arg (`SCOUT_LAUNCH_ARGS="-ScoutHoldSplash YES" ./build_run.sh`):**
- Screenshot confirms all elements rendered correctly:
  - ✅ Radar mark with green wedge (196×196, rotating)
  - ✅ "Scout" wordmark large and white (48pt bold)
  - ✅ "Find your signal." tagline dimmed beneath
  - ✅ Thin loader bar with green leading segment (40% fill)
  - ✅ Honest line near bottom in muted text
  - ✅ Near-black background, full screen, no letterbox

**Without hold arg:**
- App correctly proceeds past splash to consent screen after ~1s
- Transition animation with `.opacity` works as expected

**DoD suite:** All checks green
- ✅ Format: `./tools/bin/swiftformat Sources Tests --lint` — 0 violations
- ✅ Lint: `./tools/bin/swiftlint --strict` — 0 violations (34 files)
- ✅ Tests: `xcodebuild test` — 88 tests passed, 0 failures
- ✅ Build: successful on Scout-Sim simulator

### No limitations introduced

The splash screen is a simple, stateless view with no new trade-offs or constraints.
