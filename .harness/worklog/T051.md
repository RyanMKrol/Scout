
---
failed:blocked T051 — exhausted 2 attempts at the top model rung

---
## 2026-07-20 — completed BY HAND (post-loop, off-harness)
The loop exhausted 2 attempts and blocked T051 — root cause: it carries `visualVerify: true` on the
first-run **consent screen**, which has no force-launch seam (unlike T043's `-ScoutForceMeasuring` for
the measuring screen), so the visual-verify audit could never drive the app into the consent state to
LOOK at the rendered copy. The build/copy work itself was never the problem.

Completed manually at the owner's direction:
- `Sources/Screens/ConsentView.swift`: reworked the data-use bullets — first bullet now describes
  continuous, connection-dependent use ("potentially hundreds of MB a minute" on a fast signal, running
  total always on screen); added a bullet covering pause/stop halting data use + the ~500 MB / ~5 min
  safety auto-stop (T050 backstop). Kept the P0 foreground promise + metered-plan bullets.
- `README.md`: retired every stale "30–60 MB/min" / "paced ~256 KB transfers" / "~4×/sec rolling window"
  / "10+/5+ cap" reference across "What we can surface", "How it works", "Trade-offs", and "Core UX" —
  now describes the continuous-download + wall-clock-window + uncapped model shipped in T045–T050.
Checks: `swiftformat --lint` clean, `swiftlint --strict` 0 violations, `xcodebuild test` on Scout-Sim
green (49 files lint, 4 UI + all unit tests pass). `grep -rn "MB per minute\|MB/min" Sources README.md`
shows only the new honest wording. Status set to `done` by hand (no loop running).
