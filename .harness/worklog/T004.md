# T004 worklog

## 2026-07-17

**Status:** done

**What was done:**
Created the pure domain model types for Scout as specified in T004:
- `RadioGeneration` enum with 5G, LTE, 3G, 2G, and unknown cases
- `SignalQuality` enum with quality classification logic (great ≥ 6.0 Mbps, usable ≥ 2.0 Mbps, poor < 2.0)
- `ScoutMeter` utility enum with static display formatting and arc fraction calculations:
  - `downloadDisplay()` and `uploadDisplay()` with 10 Mbps and 5 Mbps caps respectively
  - `megabytesDisplay()` for formatting bytes as MB with smart decimal places
  - `downloadArcFraction()` and `uploadArcFraction()` for log-scale dial rendering

**Architecture decisions:**
- Made all types and methods `public` for use by later tasks
- Used `nonisolated` markers on methods and properties to opt out of MainActor isolation (appropriate for pure math functions with no UI interactions)
- Implemented `SignalQuality` equality through an extension with explicit nonisolated implementation to work around Swift 6 actor isolation rules

**Tests:**
- Created comprehensive table-driven tests covering all specified cases
- Tests verify:
  - Display formatting at boundary values and edge cases
  - Monotonic increase of arc fractions across sample ranges
  - Quality classification logic
  - Megabytes display with proper decimal/integer formatting
- All 12 domain tests + 1 scaffold test pass

**Checks run:**
- Format: `./tools/bin/swiftformat Sources Tests --lint` ✓
- Lint: `./tools/bin/swiftlint --strict` ✓
- Tests: `xcodebuild test ... Scout-Sim` ✓ (all 13 tests passed)

**Files changed:**
- `Sources/Domain/RadioGeneration.swift` (new)
- `Sources/Domain/SignalQuality.swift` (new)
- `Sources/Domain/ScoutMeter.swift` (new)
- `Tests/DomainTests.swift` (new)
