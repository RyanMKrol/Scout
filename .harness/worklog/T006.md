# T006 — Measurement seams (frozen protocols) + scenario-walk math (pure) — Worklog

## 2026-07-17 — Completed

**Task:** Freeze the measurement-source protocol contracts and implement the pure,
seeded-RNG scenario-walk math the simulated providers will later wrap (T027).

**Implementation:**
- Created `Sources/Domain/MeasurementSeams.swift`: `TransferDirection`, `ThroughputSample`,
  `ThroughputSampling`, `RadioInfoProviding`, `CellularPathMonitoring` — exactly the shapes
  specified in the task (types + protocols only, no implementations).
- Created `Sources/Domain/ScenarioWalk.swift`: `SimulationScenario` enum and the `ScenarioWalk`
  pure math namespace — `initialDownloadMbps`, `nextDownloadMbps`, `uploadMbps` — following the
  design prototype's random-walk formulas rescaled to the capped 0–10 Mbps instrument. All
  static members marked `nonisolated` (matching the existing convention in `ScoutMeter.swift` /
  `ThroughputWindow.swift`) since the project's default actor isolation is `MainActor` and this
  is pure math meant to run off the main actor in tests.
- Created `Tests/ScenarioWalkTests.swift` with a seeded `SplitMix64` `RandomNumberGenerator` and
  four tests: banded-scenario clamp-range check (200 iterations each for great/usable/poor),
  a `.great`-stays-above-floor check (≥4.55), a `.live` in-range check (200 iterations,
  [0.2, 9.9]), and an upload range + monotonic-with-download-mean check (50 draws).

**Checks run:**
- `./tools/ensure-dod-tools.sh` — pinned SwiftFormat/SwiftLint fetched.
- `xcodegen generate` — regenerated project to pick up the two new source files + the new test
  file (had to re-run after adding the test file, since the project wasn't yet aware of it).
- `./tools/bin/swiftformat Sources Tests --lint` — 0/13 files require formatting.
- `./tools/bin/swiftlint --strict` — 0 violations (fixed one large-tuple violation in the test
  file by replacing the tuple with a small private `BandCase` struct).
- `xcodebuild test -project Scout.xcodeproj -scheme Scout -destination 'platform=iOS Simulator,name=Scout-Sim' CODE_SIGNING_ALLOWED=NO`
  — all 30 tests pass, including the 4 new `ScenarioWalkTests` (up from 26 before this task).

**What remains:** T027 (the async simulated `ThroughputSampling` / `RadioInfoProviding` /
`CellularPathMonitoring` providers wrapping this math) is explicitly out of scope for this task.
