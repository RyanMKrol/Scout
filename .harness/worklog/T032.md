# T032 worklog

## 2026-07-17 — HTTP probe requests + probe pacing (pure)

### What was done

- Added `Sources/Live/HTTPProbeRequest.swift`: `enum HTTPProbeRequest` with `host`,
  `probeBytes = 262_144`, and `download()` / `uploadHeader()` / `uploadBody()` building the
  byte-exact HTTP/1.1 request strings/body from the spec.
- Added `Sources/Live/ProbePacer.swift`: `enum ProbePacer` with `interval = .milliseconds(500)`
  and `delayBeforeNextProbe(lastProbeStartedAt:now:)` — pure `ContinuousClock.Instant` arithmetic,
  `.zero` when there's no prior probe or the interval has elapsed.
- Added `Tests/HTTPProbeRequestTests.swift` (`@MainActor`, matching the project's default actor
  isolation) covering: byte-exact `download()` / `uploadHeader()` strings, `uploadBody().count ==
  262_144` with every byte `0x55`, and the three pacer cases from "## Done when" (nil last-start →
  `.zero`; 200ms elapsed → 300ms remaining; ≥500ms elapsed → `.zero`), using
  `ContinuousClock().now.advanced(by:)` to fabricate instants.
- Both new source files are pure Foundation — no sockets/URLSession/Network imports.

### Checks run

- `xcodegen generate` — regenerated project, picked up new files automatically.
- `./tools/bin/swiftformat Sources Tests --lint` — 0/42 files require formatting.
- `./tools/bin/swiftlint --strict` — 0 violations, 0 serious, 42 files.
- `./tools/loop_sim.sh && xcodebuild test -project Scout.xcodeproj -scheme Scout -destination
  "platform=iOS Simulator,name=Scout-Sim" CODE_SIGNING_ALLOWED=NO` — **TEST SUCCEEDED**, 98/98
  tests passed, 0 failures (xcresulttool summary confirmed).

### What remains

- Nothing outstanding for this task. The response parser (T018) and the live networking
  wiring that will actually use these builders/pacer are separate tasks.
