# T030 Worklog

## 2026-07-17 — RadarMarkView implementation complete

### What was done
1. Created `Sources/DesignSystem/RadarMarkView.swift` with:
   - `struct RadarMarkView: View` with `enum Style { case splash, consent }`
   - `.splash` variant: 196×196 pt with three concentric rings (r 92/62/32 pt, stroke 1.5 pt, white at opacity 0.09/0.11/0.13), center dot (r 6 pt, ScoutTheme.great), and rotating SweepWedgeView wedge behind rings (98°, opacity 0.18, period 3.2s, rotating)
   - `.consent` variant: 76×76 pt with two rings (r 35/22 pt, stroke 1.5 pt, white at opacity 0.10/0.12), smaller center dot (r 4.5 pt), and static SweepWedgeView wedge (92°, opacity 0.20, rotating: false)
   - All radii explicitly framed (no GeometryReader)

2. Updated `Sources/Screens/SplashStubView.swift` to display `RadarMarkView(style: .splash)` centered above the "Scout" wordmark in a VStack with 20pt spacing

### Checks run
- ✓ Format: `swiftformat Sources Tests --lint` — 0 files require formatting
- ✓ Lint: `swiftlint --strict` — 0 violations, 0 serious
- ✓ Test + Build: `xcodebuild test -project Scout.xcodeproj -scheme Scout -destination 'name=Scout-Sim' CODE_SIGNING_ALLOWED=NO` — 88 tests passed, build succeeded
- ✓ Visual verification: `SCOUT_LAUNCH_ARGS="-ScoutHoldSplash YES" ./build_run.sh` → screenshot shows splash stub with radar mark (three faint concentric rings, green center dot, visible soft green rotating wedge, on near-black background)

### Verification
- Frozen initializer compiles exactly as specified: `struct RadarMarkView: View { enum Style { case splash, consent } init(style: Style) }`
- All Definition of Done requirements met
- App fills entire screen (no letterbox)
