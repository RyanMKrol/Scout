# T021 worklog

## 2026-07-17 — Initial implementation

### What was done

1. **Updated `project.yml`** to add the `ScoutUITests` target with the correct settings:
   - `type: bundle.ui-testing`, `platform: iOS`, `sources: [UITests]`, `dependencies: [target: Scout]`
   - Settings: `PRODUCT_BUNDLE_IDENTIFIER: com.ryankrol.scout.uitests`, `GENERATE_INFOPLIST_FILE: YES`, `SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated`
   - Added `ScoutUITests` to the `Scout` scheme's `test` targets (alongside `ScoutTests`)
   - Added `ScoutUITests` to `build` with `[test]`

2. **Created `UITests/ScoutUITests.swift`** with four XCUITest tests following house rules:
   - All queries use `accessibilityIdentifier` only, never display copy
   - All existence checks use bounded waits with `waitForExistence(timeout: 15)`
   - No exact value assertions (Mbps readings are random-walk driven)
   - Each test sets explicit `app.launchArguments`:
     - `testFirstRunConsentFlow`: Tests consent flow with args `["-firstRunConsentGiven", "NO"]`
     - `testMeasuringScreenElements`: Tests measuring screen with args `["-firstRunConsentGiven", "YES", "-ScoutScenario", "Great"]`
     - `testNoCellularState`: Tests no-cellular fallback with args `["-firstRunConsentGiven", "YES", "-ScoutScenario", "None"]`
     - `testDeclineThenStartFromIdle`: Tests decline-then-start flow with args `["-firstRunConsentGiven", "NO"]`

3. **Generated project** with `xcodegen generate` — Scout.xcodeproj updated with the new UI tests target.

### Checks run

- **Format check** (`swiftformat Sources Tests UITests --lint`): ✅ Passed (0 files require formatting)
- **Lint check** (`swiftlint --strict`): ✅ Passed (0 violations in 39 files)
- **Full test suite** (`xcodebuild test` on Scout scheme):
  - Unit tests (ScoutTests): 88 tests ✅ Passed
  - UI tests (ScoutUITests): 4 tests ✅ Passed
  - Total: 92 tests executed, 0 failures

All tests executed against the dedicated `Scout-Sim` simulator (no generic model names used).

### Test outcomes

All four UI tests passed:
- `testFirstRunConsentFlow`: Tests splash → consent screen flow → tap start → measuring screen
- `testMeasuringScreenElements`: Tests that all measuring screen elements appear (hero, upload, quality, dataDown, dataUp)
- `testNoCellularState`: Tests that paused state appears when cellular unavailable, and measuring.quality does NOT appear
- `testDeclineThenStartFromIdle`: Tests consent decline → paused state → tap start → measuring screen

### Definition of Done

✅ `xcodegen generate` succeeds with the new target  
✅ Four tests exist and PASS in the standard DoD run (`xcodebuild test` on Scout scheme)  
✅ No test queries display copy or asserts numeric reading values  
✅ All waits are bounded (15s timeout on all waitForExistence calls)  
✅ Full DoD suite (format, lint, tests, build) passes locally
