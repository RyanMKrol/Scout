# T009 worklog

## 2026-07-17

Built `Sources/App/AppEnvironment.swift`: a `@MainActor` enum composition root with
`scenario()` (parses `UserDefaults.standard.string(forKey: "ScoutScenario")` via
`SimulationScenario(rawValue:)`, defaulting to `.live` on a missing key or unknown string) and
`makeSession()` (builds a `SweepSession` wired to `SimulatedSampler`, `SimulatedRadioProvider`,
and `SimulatedPathMonitor`, each construction line carrying a seam comment marking where the
on-device live provider swaps in for later tasks T016/T017/T019).

Added `Tests/AppEnvironmentTests.swift` covering: each raw scenario value ("Live"/"Great"/
"Usable"/"Poor"/"None") maps to its case, a missing key defaults to `.live`, a garbage string
defaults to `.live`, and `makeSession()` compiles/returns a session. Defaults key is removed in
`tearDown()` so no test state leaks into standard `UserDefaults`.

Checks run (all green):
- `./tools/ensure-dod-tools.sh`
- `xcodegen generate`
- `./tools/bin/swiftformat Sources Tests --lint` — 0/25 files require formatting
- `./tools/bin/swiftlint --strict` — 0 violations
- `xcodebuild test -project Scout.xcodeproj -scheme Scout -destination 'platform=iOS Simulator,name=Scout-Sim' CODE_SIGNING_ALLOWED=NO` — 73 tests, 0 failures (includes the 4 new `AppEnvironmentTests`)

Nothing remains for this task; RootView/screen wiring that will call `AppEnvironment.makeSession()`
is out of scope here and lands in a later task.
