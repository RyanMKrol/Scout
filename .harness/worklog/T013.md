# T013 — SweepDial: the dual-arc radar dial assembly

## 2026-07-17

**Completed:** T013 SweepDial assembly.

### What was done

1. Created `Sources/DesignSystem/SweepDialView.swift` with the exact input surface spec:
   - `enum Mode: Equatable { case live(DialContent), idle }`
   - `init(mode: Mode)`

2. Layered construction in 288×288 pt ZStack:
   - Sweep wedge (258 pt) with quality color at 0.14 opacity, 92°, period 3.4s, rotating
   - Download track (264 pt diameter, 7 pt stroke, white 0.07)
   - Download arc with dual animations: arc length on `downFraction` (.linear 0.28s) + color on `qualityColor` (.easeInOut 0.45s)
   - Upload track (200 pt diameter, 6 pt stroke, white 0.06)
   - Upload arc with arc-length animation on `upFraction`, fixed `uploadArc` color (no color animation)
   - Center stack passthrough via `DialCenterStack(mode:)`

3. Dynamic Type scaling via `@ScaledMetric(relativeTo: .largeTitle)` on dialDiameter (base 288)

4. Wedge maintains stable identity (never inside conditional flips per tick); per-tick changes flow through `DialContent`

5. Wired into `MeasuringStubView`:
   - Replaced bare `DialCenterStack` with `SweepDialView(mode: .live(content))`
   - Replaced idle center with `SweepDialView(mode: .idle)`

### Verification

**Format/Lint:** ✅ SwiftFormat + SwiftLint pass, 0 violations

**Build + Tests:** ✅ All 88 tests pass

**Visual verification (3 scenarios):**
- `-ScoutScenario Great`: Green outer arc (0.5 fill), blue inner arc, soft wedge, 5G label, hero 8.2 Mbps ✅
- `-ScoutScenario Poor`: Clay/terracotta outer arc (0.2 fill, visibly shorter), blue inner arc, LTE label, hero 0.8 Mbps ✅
- `-ScoutScenario None`: Idle dial—dim track ring only, em dash, "Mbps", no arcs, no wedge ✅

### DoD checklist

- [x] Frozen `SweepDialView` surface compiles exactly as written
- [x] Format + lint green (0 violations)
- [x] All tests pass (88/88)
- [x] Build succeeds
- [x] Visual verification: all 3 scenarios render correctly

**Status:** ✅ DONE
