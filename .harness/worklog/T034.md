# T034 — Dark-mode-only enforcement + dark launch screen

**Built:** 2026-07-17 17:32 UTC

## Changes Made

1. **project.yml**: Updated Scout target's `info.properties`:
   - Changed `UILaunchScreen: {}` to `UILaunchScreen: { UIColorName: LaunchBackground }` to reference the launch color asset
   - Added `UIUserInterfaceStyle: Dark` to pin the app to dark appearance

2. **Sources/Assets.xcassets/LaunchBackground.colorset/Contents.json**: Created new color asset
   - Universal sRGB color with components: red `0x05`, green `0x05`, blue `0x06`, alpha 1.0
   - Matches design background `#050506`

3. **Sources/App/RootView.swift**: Added `.preferredColorScheme(.dark)` to the root view's top-level content for belt-and-braces dark mode enforcement

## Verification

- **xcodegen generate**: Successfully generated Scout.xcodeproj
- **SwiftFormat**: 0/46 files require formatting (lint mode)
- **SwiftLint**: Found 0 violations, 0 serious in 46 files
- **xcodebuild test**: All 4 tests passed (8.02s unit + 43.94s UI = 51.96s total)
- **plutil Info.plist verification**:
  ```
  "UILaunchScreen" => {
    "UIColorName" => "LaunchBackground"
  }
  "UIUserInterfaceStyle" => "Dark"
  ```
- **build_run.sh**: Built successfully (exit code 0)
- **Screenshot**: App renders full-screen (1179 x 2556) in dark mode, no letterbox regression

All Definition of Done checks passed. Colorset committed, all tools green.
