# T028 — RootView: routing UI, splash overlay, stub screens

## Overview

Wire the routing shell: the `@main` App shows RootView, which overlays a splash, routes
splash → consent → measuring via AppRouter, persists consent, and injects the session. Screens are
minimal STUBS with stable accessibility identifiers — the real screens replace them in later tasks.

## Do

1. `Sources/App/RootView.swift` + update the `@main` App entry (`Sources/ScoutApp.swift`) to show it:
   - `@AppStorage("firstRunConsentGiven") private var consentGiven = false` (a
     `-firstRunConsentGiven NO`/`YES` launch argument overrides it automatically via the
     UserDefaults argument domain).
   - Create the router once: `@State private var router = AppRouter(consentGiven: <initial>,
     holdSplash: UserDefaults.standard.bool(forKey: "ScoutHoldSplash"))`.
   - Own the session: `@State private var session = AppEnvironment.makeSession()`, injected with
     `.environment(session)`.
   - ZStack: content for `router.phase` (`.splash` → SplashStubView, `.consent` → ConsentStubView,
     `.measuring` → MeasuringStubView). The splash overlays with `.zIndex(1)` and
     `.transition(.opacity)`; after a 1 s `.task` sleep call
     `withAnimation(.easeOut(duration: 0.4)) { router.splashFinished() }` (the router's `holdSplash`
     makes that a no-op when the hold flag is set — used by screenshot tooling).
   - Consent wiring: onStart → `router.startSweeping()`; onNotNow → `router.declineConsent()`.
     Mirror `router.consentGiven` back into the `@AppStorage` var on change.
   - **Start the session when the app ENTERS measuring with consent** (grant moment or
     splash-finish for a returning user): call `session.start()`. Do NOT call it when consent is
     false. (Background/foreground stop-start is the NEXT task; do not add scenePhase handling here.)
2. Stub screens in `Sources/Screens/` — deliberately minimal, on `ScoutTheme.background` full-screen
   (`.ignoresSafeArea()`), with these accessibility identifiers (UI tests bind to them later):
   - `SplashStubView`: text "Scout" — id `splash.wordmark`.
   - `ConsentStubView(onStart:onNotNow:)`: a title text + button "Start sweeping" (id
     `consent.start`) + button "Not now" (id `consent.notNow`).
   - `MeasuringStubView`: shows `ScoutMeter.downloadDisplay(session.downloadMbps)` (id
     `measuring.hero`); when `!session.cellularAvailable` OR consent is not given, instead show text
     "PAUSED" (id `paused.title`) and, if consent not given, a "Start sweeping" button (id
     `paused.startButton`) calling the same start action.
3. Delete the T001 "Hello, Scout" placeholder content from `Sources/ContentView.swift` (remove the
   file and its references, or repurpose it) — the app now launches into RootView.

## Done when

- `./build_run.sh` screenshot shows the stub flow: on a fresh install the consent stub (after the
  splash passes); LOOK at the screenshot. (Scenario/paused forcing needs the launch-arg plumbing of
  the NEXT task — only the default path must be visually confirmed here.)
- Router unit tests from T008 still pass unchanged; the app builds and runs with RootView as the
  entry; standard DoD run green.
