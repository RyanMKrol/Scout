# T011 — Splash screen (full fidelity)

## Overview

Replace the splash stub with the real splash screen: radar mark sweeping, wordmark, tagline, loader
bar, and the honest line — pixel-per the handoff (`design/design_handoff_scout/README.md` §1 and the
`Scout.dc.html` prototype's 2a panel).

## Do

Replace `SplashStubView` usage with a full `Sources/Screens/SplashView.swift` (delete or repurpose the
stub file; keep the routing behavior from RootView unchanged — including the `ScoutHoldSplash` hold):

- Full-screen `ScoutTheme.background`, `.ignoresSafeArea()`.
- Centered VStack: `RadarMarkView(style: .splash)` (bottom margin 44), then wordmark, then tagline:
  - Wordmark: `Text("Scout")`, id `splash.wordmark`, `.font(.system(size: 48, weight: .bold))`,
    `.tracking(-1.5)`, white.
  - Tagline: "Find your signal." — 17 pt medium, `ScoutTheme.white(0.5)`, 12 pt below wordmark.
- Pinned near the bottom (64 pt above the bottom edge), centered, 18 pt gap:
  - Loader: 132×3 pt pill (`Capsule`), track `ScoutTheme.white(0.12)`, leading-aligned fill 40% width
    (52.8 pt) in `ScoutTheme.great`, also a capsule. Static fill (no progress animation needed).
  - Honest line: "Measures real usable speed — not signal bars" — 12 pt, `ScoutTheme.white(0.3)`.
- Decorative elements (mark, loader) hidden from accessibility (`.accessibilityHidden(true)`); the
  wordmark stays an accessible text.

## Done when

- `SCOUT_LAUNCH_ARGS="-ScoutHoldSplash YES" ./build_run.sh` → screenshot shows ALL of: radar mark
  with green wedge, "Scout" wordmark large and white, "Find your signal." dimmed beneath, the thin
  loader bar with a green leading segment, and the honest line near the bottom — on near-black,
  nothing clipped. LOOK at the screenshot and confirm each element.
- Without the hold arg, the app still proceeds past splash after ~1 s to consent/measuring.
- Standard DoD run green.
