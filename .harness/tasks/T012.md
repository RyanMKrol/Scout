# T012 — First-run consent screen (full fidelity)

## Overview

Replace the consent stub with the real data-use consent screen — the one intentional pause before
measuring. Exact copy and layout per `design/design_handoff_scout/README.md` §2 (prototype panel 3a).
"Start sweeping" grants consent and enters measuring; "Not now" is a real choice that enters the idle
(non-transferring) measuring state.

## Do

Create `Sources/Screens/ConsentView.swift` (replacing the stub in RootView's `.consent` branch):

```swift
struct ConsentView: View { init(onStart: @escaping () -> Void, onNotNow: @escaping () -> Void) }
```

- Full-screen `ScoutTheme.background`. Content top-aligned: ~132 pt top padding, 32 pt sides; buttons
  pinned to the bottom (48 pt bottom padding) with a Spacer between.
- `RadarMarkView(style: .consent)`, 32 pt below it the title.
- Title (id `consent.title`): "Scout uses cellular data to measure" — 30 pt bold, `.tracking(-0.6)`,
  white, wrapping naturally (2 lines).
- Body, 16 pt below title: "To read real, usable speed — both download and upload — Scout transfers a
  little data over your cellular connection while it's running." — 16 pt, `ScoutTheme.white(0.6)`,
  line spacing ~1.55 relative.
- Three bullets, 32 pt below body, 18 pt apart. Each: 6 pt green dot (`ScoutTheme.great`, aligned to
  first text line) + 13 pt gap + 15 pt text in `ScoutTheme.white(0.72)`:
  1. "Roughly **10–40 MB per minute**, always shown on screen." — the "10–40 MB per minute" span
     white 100%, semibold, `.monospacedDigit()`. (Build with `Text` concatenation or AttributedString.)
  2. "Only while the screen is on and you're looking — never in the background."
  3. "On a metered or low-data plan? Keep sessions short."
- Buttons (bottom, 14 pt apart):
  - Primary (id `consent.start`): "Start sweeping" — full width, height 56, corner radius 16, fill
    `ScoutTheme.great`, label 18 pt bold, color `ScoutTheme.onAccent`. Calls `onStart`.
  - Secondary (id `consent.notNow`): "Not now" — height 44, centered, 16 pt medium,
    `ScoutTheme.white(0.5)`, no fill. Calls `onNotNow`.
- Buttons keep ≥44 pt hit targets; text supports Dynamic Type scaling (buttons stay pinned; content
  scrolls into a ScrollView if it would overflow at accessibility sizes).

## Done when

- `SCOUT_LAUNCH_ARGS="-firstRunConsentGiven NO" ./build_run.sh` → screenshot shows the full consent
  screen: small static radar mark, exact title, body, three green-dotted bullets with the bold MB
  figure, green primary button and quiet "Not now" — matching the layout described above. LOOK at
  the screenshot and confirm.
- The two closures route exactly as before (router tests from T008 still pass; stub file removed).
- Standard DoD run green.
