# T045 — Continuous streaming download sampler (replace discrete paced probes)

## Overview
Scout must read throughput like a live speedometer: to measure a rate you need data actually flowing.
Today the sampler sends tiny discrete 256 KB request/response probes with gaps between them
(`Sources/Live/CellularThroughputSampler.swift` — `ProbeSchedule.direction` 3:1, `ProbePacer.interval`
250ms, `runDownloadProbe` → `receiveUntilComplete`), so between probes nothing flows and the reading
collapses; each tiny probe also just pegs the cap. This task replaces that with ONE continuous,
unbounded download that streams indefinitely, emitting a `ThroughputSample` per received chunk so the
window can compute a true wall-clock rate. **This supersedes the paced-probe approach shipped in T038 —
do not preserve the discrete-probe / ProbePacer / 3:1-schedule download path; replace it.**

## Do
Rework `Sources/Live/CellularThroughputSampler.swift` so its download path is a single persistent
transfer over the forced-`.cellular` `NetworkConnection`: request a very large / effectively unbounded
body (`GET /__down?bytes=<large>` from `speed.cloudflare.com` via `HTTPProbeRequest` — raise/replace
`probeBytes` for the download so it streams for the whole session, not 262144), then loop on
`connection.receive(...)` continuously, and for EACH received chunk `yield` a `ThroughputSample` whose
`byteCount` is that chunk's size and whose `endedAt` is the wall-clock instant the chunk arrived (the
`transferDuration` field is no longer meaningful for the wall-clock model — set it to the small delta
since the previous chunk or `.zero`; the new window T046 ignores it). Keep receiving until the stream
ends or the task is cancelled; if the endpoint closes the fixed-size body, transparently re-request and
continue so the stream is effectively unbounded. Preserve the `ThroughputSampler` protocol seam and the
`AsyncStream<ThroughputSample>` return of `samples()` so the rest of the app and the scripted fake still
work; update the fake (`Sources/Domain/SimulatedProviders.swift`) to emit a continuous stream of
per-chunk download samples (so tests exercise the new continuous shape, not discrete probes). Upload is
handled separately (T048) — this task may leave a minimal/stubbed upload path or none; do not block on it.

## Done when
- The live download path is a single continuous streaming receive loop (no `ProbePacer`-gated discrete
  256 KB download probes remain), emitting a `ThroughputSample` per received chunk with `byteCount` =
  chunk size and `endedAt` = arrival instant, over the forced-`.cellular` interface.
- `samples()` still returns `AsyncStream<ThroughputSample>` and the `ThroughputSampler` seam is intact;
  the scripted fake in `Sources/Domain/SimulatedProviders.swift` emits a continuous per-chunk stream.
- A unit test (using the fake, injected clock) asserts the sampler surfaces many incremental chunk
  samples over a continuous run (not one-sample-per-probe), each carrying a positive `byteCount` and a
  monotonically advancing `endedAt`.
- On device (manual, T025-style): with a sweep running, data streams continuously (the session data
  counter climbs steadily, not in ~256 KB steps).

## Notes
- Do NOT edit `.harness/**`. The now-outdated "capped instrument" / "10–40 MB/min" limitation-ledger
  rows are updated by the human integrator at merge.
