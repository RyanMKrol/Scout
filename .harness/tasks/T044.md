## Overview
The main re-attempt of T042. The first attempt was BLOCKED at the very first build for scope-creep: it edited `Sources/Domain/AccessibilitySummary.swift` (to reflect the paused state in the VoiceOver accessibility value — `AccessibilitySummary.value(...)` is the accessibilityValue used in MeasuringView.measuringContent, line ~47), but T042's scope was only `Sources/Screens/**`, `Sources/DesignSystem/**`, `Sources/App/**` — it excluded `Sources/Domain/`. This re-scope adds `Sources/Domain/**` and `Tests/**` so the paused accessibility value and the new UI test are in-scope. The SweepSession pause/resume API this needs already shipped in T041 and is merged: `session.isPaused` (Bool), `session.pause()`, `session.resume()`, and `session.togglePause()` — call them directly.

## Do
In the live measuring content (MeasuringView.measuringContent / SweepDialView live mode, using SweepDialView / DialCenterStack), add a tap target over the central dial that calls `session.togglePause()`. Give the tappable dial area a stable `accessibilityIdentifier` `dial.pauseToggle`. When `session.isPaused` is true: freeze the readout, show a clear paused visual indication on the dial, and change the status row from 'SWEEPING' to a paused affordance — but give the paused indicator a NEW `accessibilityIdentifier` `dial.paused`, distinct from the existing empty-state `paused.status` (that identifier belongs to the no-consent/no-cellular empty state and must keep resolving unchanged). The tap gesture must be active ONLY in the live sweep state, never in `pausedContent()` (the empty states). For VoiceOver: expose the pause/resume as an accessibility action and reflect the paused state in the accessibility value — this is where you may edit `Sources/Domain/AccessibilitySummary.swift` (e.g. add a paused variant or a paused-aware value), which is now in scope. Leave `visualVerify` unset — the loop's visual hook auto-covers this ui/feature task. The UI test relies on the `-ScoutForceMeasuring` launch-arg seam added in unit review-t042-a to reach the live measuring state.

## Done when
Tapping the central dial while sweeping freezes the readout and reveals a paused indicator identified by the NEW `dial.paused` accessibilityIdentifier (distinct from the empty-state `paused.status`); tapping again resumes live updates; the toggle is inactive/absent in the no-consent and no-cellular empty states (those screens stay visually unchanged and `paused.status` / `paused.title` / `paused.startButton` still resolve). A NEW XCUITest (XCUIApplication) in Tests/ launches the app with the `-ScoutForceMeasuring 1` launch argument (from unit review-t042-a) so it starts in the live measuring state, queries the dial toggle by the `dial.pauseToggle` accessibilityIdentifier, taps it, asserts the `dial.paused` element appears (bounded wait via `waitForExistence`, matched by identifier not display copy), taps again, and asserts `dial.paused` disappears. If — despite unit review-t042-a's seam — an XCUITest still cannot be driven headlessly in this harness (e.g. the runner has no UI-testing host), the fallback bar is a plain XCTest unit test asserting the toggle wiring on the view model / SweepSession (tap → `isPaused` flips true, tap again → false, and the empty-state path never enables the toggle) plus a unit assertion that `AccessibilitySummary` yields a paused-aware value; state in the worklog which of the two was used and why.
