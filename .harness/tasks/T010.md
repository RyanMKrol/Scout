# T010 — Animated primitives: SweepWedgeView + PulsingDot

## Overview

The two repeat-forever animated primitives every screen reuses: the rotating angular-gradient sweep
wedge and the pulsing status dot. Built as isolated leaf views so the measuring screen's ~4 Hz state
churn can never reset their animations. Not yet visible in the running app — the radar mark task
(next) puts the wedge on screen; verify these via previews + build.

## Do

Both in `Sources/DesignSystem/`. Reference design: `design/design_handoff_scout/README.md`.

1. `SweepWedgeView.swift`:

```swift
struct SweepWedgeView: View {
    init(color: Color, opacity: Double, wedgeDegrees: Double, period: Double, rotating: Bool)
}
```

   - A circle filled with an `AngularGradient` built from STOPS over the full turn (an
     endAngle < 360° does NOT leave the rest transparent — it clamps to the nearest stop):
     `[.init(color: color.opacity(0), location: 0), .init(color: color, location: wedgeDegrees/360),
     .init(color: color.opacity(0), location: wedgeDegrees/360 + 0.0001), .init(color:
     color.opacity(0), location: 1)]`. Fade to `color.opacity(0)`, NEVER `.clear` (transparent black
     fringes gray). Apply `opacity` to the whole view.
   - Rotation: `@State private var isRotating = false`, `.rotationEffect(.degrees(isRotating ? 360 :
     0))`, `.animation(.linear(duration: period).repeatForever(autoreverses: false), value:
     isRotating)`, `.onAppear { isRotating = true }`. This keyed-value idiom is load-bearing: never
     use un-keyed `.animation`, never `withAnimation` in `onAppear`, and keep this view's inputs
     stable so parent re-renders can't reset it.
   - `rotating: false` OR `@Environment(\.accessibilityReduceMotion) == true` → no rotation (static
     wedge, no animation applied).

2. `PulsingDot.swift`:

```swift
struct PulsingDot: View { init(color: Color, diameter: CGFloat = 7) }
```

   - Circle; pulse per the handoff (`0.3 opacity / 0.8 scale → 1 / 1 → back`, full cycle
     `ScoutMotion.pulsePeriod`): `.animation(.easeInOut(duration: ScoutMotion.pulsePeriod / 2)
     .repeatForever(autoreverses: true), value: pulsing)` with the same keyed `onAppear` idiom.
   - Reduce Motion → static at full opacity/scale.

3. Give each a `#Preview` on `ScoutTheme.background` so the components are inspectable in isolation.

## Done when

- Both views compile with exactly the frozen initializers; the gradient is stop-based as specced;
  reduce-motion branches exist; previews present.
- Standard DoD run green. (On-screen visual confirmation lands with the radar-mark task, which
  composes the wedge into the splash stub.)
