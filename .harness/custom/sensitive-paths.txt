# sensitive-paths.txt — extra pre-push secret-guard patterns for Scout.
# One extended-regex (ERE) fragment per line; blank lines and #-comments ignored. OR-appended to the
# built-in guard (can only ADD protection). Check a path with:
#   .harness/scripts/loop.sh --guard-selftest <path>
#
# Scout is simulator-only today (no signing needed), but these Apple-material guards are in place NOW
# so they already protect the repo the moment signing / App Store distribution is added.

# App Store Connect API keys and Apple signing material — should never live in the repo at all.
\.p8$
\.p12$
\.mobileprovision$
\.cer$

# Local signing identity (DEVELOPMENT_TEAM), if/when Scout adopts the committed-stub +
# gitignored-local Signing.xcconfig pattern.
(^|/)Signing\.local\.xcconfig$
